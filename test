inp = input("Enter the list elements:")
list = []
for i in inp:
	length=(len(inp))
	list.append(i)
	
	for j in list:
		if j==inp[length-1]:
			list.remove(j)
		elif j == inp[length-2]:
			list.remove(j)
print(list)